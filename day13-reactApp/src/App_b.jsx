import { useState } from "react"

const App_b = () => {
    const [flag, setFlag] = useState(true)
    const handleClick = () => {
        setFlag(False)
    }

    return (
        <section>

            {flag === True ? (
                
            ) : (

            )}
        </section>
    )
}