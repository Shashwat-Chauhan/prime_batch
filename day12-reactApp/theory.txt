CommonJS Format(mostly used in old node/express apps)
    module.exports = temp;
    const temp = require("./)

E5 Modules (all modern apps)
    export temp;
    import temp from "./"

    import React from 'react';
    imort ReactDOM from 'react-dom'

In your script tag , write type = module
In your package.json, write type: module

Install a bundler :
npm i parcel

